from .db_base import Base
from .db_config import DBConnectionHandler

"""
    Declaramos que apenas poderá ser usada para fora desse módulo o Base (de db_base)
    e DBConnectionHandle (de db_config)
"""

""" barramento no init """
